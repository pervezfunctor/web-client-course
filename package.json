{
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "scripts": {
    "update": "pnpm add -g @pnpm/exe && pnpm ncu -u && pnpm i",
    "types": "pnpm turbo types",
    "lint": "pnpm turbo lint",
    "build": "pnpm turbo build",
    "test": "pnpm turbo test",
    "dev": "pnpm turbo dev",
    "ci": "pnpm run types lint"
  }
}
