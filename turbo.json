{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["$NODE_ENV"],

  "pipeline": {
    "build": {
      "outputs": ["types", "dist/**"]
    },
    "lint": {
      "outputs": []
    },
    "types": {
      "outputs": []
    },
    "test": {
      "outputs": []
    }
  }
}
